# Terraform Outputs

This folder is reserved for generated output files when running `terraform output -json > terraform/outputs/stage.json`. Keep environment-specific state information outside of version control.
